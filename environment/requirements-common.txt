# ===== 通用轻量依赖（Mac + AutoDL 两边都装）=====
# 这些包没有 CUDA / torch 硬依赖，纯 Python 或自带 CPU wheel

# --- LLM API 客户端 ---
openai>=1.30.0
langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0

# --- 向量库（Python 原生，不依赖 torch）---
faiss-cpu>=1.8.0
chromadb>=0.5.0

# --- 模型下载（两个平台都需要拉资源）---
huggingface_hub>=0.23.0
modelscope>=1.18.0

# --- 工具 ---
pyyaml>=6.0
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0
pillow>=10.0.0
tiktoken>=0.7.0
python-dotenv>=1.0.0
tqdm>=4.66.0
jupyter>=1.0.0
ipykernel>=6.29.0

# 注意：sentence-transformers 是 torch 的上层封装
#   Mac 本地跑 embedding → 装 requirements-mac.txt（含 CPU 版 torch）
#   AutoDL 训练微调    → 装 requirements-gpu.txt（含 CUDA 版 torch）
