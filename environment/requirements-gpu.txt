# ===== AutoDL GPU 依赖（只 AutoDL 装）=====
# 训练 / 微调 / 大模型推理必需，体积大、强依赖 CUDA

# PyTorch 2.5.1（如需 CUDA 版需从 PyTorch 官方 index 安装）
# 默认 pip 源装的是 CPU-only 版；CUDA 版安装命令:
#   pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 \
#       --index-url https://download.pytorch.org/whl/cu124
# setup-autodl.sh 直接 pip install -r 本文件，未指定 CUDA index，
# 若需要 GPU 加速请手动加 --index-url 或修改 setup-autodl.sh
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# HF 生态 + 微调核心
# transformers 各依赖约束交集：
#   vllm 0.7.3           -> >=4.48.2
#   unsloth_zoo 2025.3.17 -> >=4.46.1, !=4.47.0（无上限）
#   ⚠️ 4.52.0~4.55.1 含多个被 yank/拉黑的版本，避开
# transformers 4.51.x 要求 Python >=3.9；Python 3.8 无法安装
transformers==4.51.3
accelerate==1.7.0
peft==0.15.0                         # 满足 unsloth_zoo 2025.3.17 的 >=0.7.1, !=0.11.0
trl==0.15.2                          # 满足 unsloth_zoo 2025.3.17 的 <=0.15.2
datasets==3.1.0  # 排除 breaking change 的 4.0/4.1.0/5.0.0；5.0+ 与 modelscope 1.39 as_dataset 不兼容

# modelscope 备用国内模型源
modelscope>=1.18.0

# 量化（bitsandbytes 在 Mac 上编译不了，只 Linux GPU 可用）
bitsandbytes==0.45.4

# 微调加速（锁版本，两版本配套；只支持 Linux + CUDA，Mac 不可用）
# unsloth 2025.3.19 -> 依赖 unsloth_zoo>=2025.3.17；zoo 2025.3.17 无 torchao 依赖
unsloth==2025.3.19
unsloth_zoo==2025.3.17
# ⚠️ 注：setup-autodl.sh 中有卸载 torchao 的逻辑，那是为未来升级到
#    unsloth_zoo 2025.10.x 做准备（该版本硬绑 torchao 且与 torch 2.5.1 不兼容）
#    当前锁定的 2025.3.17 并不依赖 torchao，卸载操作是无害的冗余

# 推理引擎
vllm==0.7.3                       # 0.11 硬绑 torch==2.8，降版到 0.7.3 兼容 torch 2.5.1

# 实验追踪
wandb==0.19.11

# 基础库
numpy==1.26.4
scipy==1.13.1            # 兼容 numpy 1.26.x；scipy>=1.14 需要 numpy>=2.0
packaging==25.0
pandas==2.2.0


