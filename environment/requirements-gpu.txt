# ===== AutoDL GPU 依赖（只 AutoDL 装）=====
# 训练 / 微调 / 大模型推理必需，体积大、强依赖 CUDA

# CUDA 版 PyTorch（cu124 / CUDA 12.4）
# 如需强制 CUDA 版本，setup-autodl.sh 里会用:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# HF 生态 + 微调核心
transformers==4.51.3
accelerate==1.7.0
peft==0.15.0
trl==0.15.2
datasets==3.1.0
sentence-transformers==3.3.0

# modelscope 备用国内模型源
modelscope>=1.18.0

# 量化（bitsandbytes 在 Mac 上编译不了，只 Linux GPU 可用）
bitsandbytes==0.45.4

# 微调加速（锁版本，两版本配套；只支持 Linux + CUDA，Mac 不可用）
# unsloth_zoo 2025.3.17 不依赖 torchao，兼容 torch 2.5.1
unsloth==2025.3.19
unsloth_zoo==2025.3.17

# 推理引擎
vllm==0.7.3

# 实验追踪
wandb==0.19.11

# 基础库
numpy==1.26.4
scipy==1.13.1            # 兼容 numpy 1.26.x；scipy>=1.14 需要 numpy>=2.0
packaging==25.0
pandas>=2.2.0
